from setuptools import setup, find_packages

setup(
    name="cdp_langchain",
    version="0.1",
    packages=find_packages(),  # This will automatically detect `cdp_langchain` and `cdp_langchain.utils`
    description="CDP Langchain package",
    author="Your Name",
    author_email="your.email@example.com",
    license="MIT",
)
